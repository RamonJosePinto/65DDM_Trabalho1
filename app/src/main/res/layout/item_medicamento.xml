<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:backgroundTint="@android:color/white">

    <!-- Nome do Medicamento -->

    <!-- Botão de Edição -->

    <!-- Botão de Deleção -->

    <ImageButton
        android:id="@+id/button_editar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:background="?android:attr/selectableItemBackground"
        android:contentDescription="Editar"
        android:src="@drawable/icone_edit_35dp"
        app:layout_constraintBottom_toBottomOf="@+id/text_view_nome_medicamento"
        app:layout_constraintEnd_toStartOf="@+id/button_deletar"
        app:layout_constraintTop_toTopOf="@+id/text_view_nome_medicamento" />

    <TextView
        android:id="@+id/text_view_nome_medicamento"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:text="Medicamento"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/button_deletar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="?android:attr/selectableItemBackground"
        android:contentDescription="Deletar"
        android:src="@drawable/icone_delete_35dp"
        app:layout_constraintBottom_toBottomOf="@+id/text_view_nome_medicamento"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/text_view_nome_medicamento"
        app:layout_constraintVertical_bias="0.454" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?actionBarDivider"
        android:layout_marginTop="14dp"
        app:layout_constraintTop_toBottomOf="@+id/button_editar"
        tools:layout_editor_absoluteX="0dp" />


</androidx.constraintlayout.widget.ConstraintLayout>
